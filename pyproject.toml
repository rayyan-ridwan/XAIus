[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "xaius"
version = "0.1.0"
description = "Explainable AI model selector"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["xaius"]
